# cd tools

# cargo run --release --bin gen seeds.txt --dir=

# - `--dir=in2` 入力ファイルの出力先を `in` ではなく、指定されたものに変更
# - `--M=10` 生成される入力の $M$ の値を指定されたものに固定
# - `--eps=0.01` 生成される入力の $\epsilon$ の値を指定されたものに固定